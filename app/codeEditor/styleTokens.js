var colorTokens = tokens => tokens.map(token => {
    switch (token.getCategory()) {
        case "PRE_PROCESSOR": return token;
        case "COMMENT": return token.addClass();
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
        case "PRE_PROCESSOR": return token;
    }
});